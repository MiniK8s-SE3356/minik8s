apiVersion: v1
kind: Dns
metadata:
  name: dns-test
spec:
  host: dns.test.com
  paths:
  - subPath: /clusterip1
    svcName: dns-test-clusterip1
    svcPort: 9200
  - subPath: /clusterip2
    svcName: dns-test-clusterip2
    svcPort: 9201
